# コミカル・ドッジバトル

Windowsパソコンのブラウザで遊べる、斜め見下ろし型の2Dドッジボールアクションゲームです。HTML、CSS、JavaScriptとCanvas APIだけで作っています。

## 起動方法

1. このフォルダの `index.html` をブラウザで開きます。
2. タイトル画面で `Space` キー、またはゲームコントローラーのボタン1を押すと試合開始です。

サーバーは不要です。

## 操作方法

### キーボード

| 操作 | キー |
| --- | --- |
| 移動 / 照準 | WASD または 矢印キー |
| ボタン1: パス / しゃがみ / 開始 | Space |
| ボタン2: シュート / キャッチ | C |
| ボタン3: ジャンプ | J |
| ダッシュ | 方向キーまたはWASDを同じ方向に二度押し / Shift |
| ポーズ | Escape |

### ゲームコントローラー

| 操作 | ボタン |
| --- | --- |
| 移動 / 照準 | 左スティック または 方向キー |
| 操作キャラ変更 | 右スティック |
| ボタン1: パス / しゃがみ / 開始 | ゲームパッド番号1 |
| ボタン2: シュート / キャッチ | ゲームパッド番号2 |
| ボタン3: ジャンプ | ゲームパッド番号3 |
| ダッシュ | 左スティックを同じ方向に二度押し / ゲームパッド番号4 |
| ポーズ | STARTボタン |

コントローラーのボタン番号は `input.js` の `gamepadMap` で変更できます。

## 新しいルール

- 両チームとも、コート内に3人、コート外に3人います。
- コート内の3人にはHPがあり、キャラクターの頭上にHPバーが表示されます。
- コート外のキャラクターは外野役です。HPバーは表示されません。
- ボール保持者やボールの位置に合わせて、画面が左右にスクロールします。
- ボールが落ちているときは、自チーム内野のうちボールに近いキャラクターへ操作カーソルが自動で切り替わります。
- 右スティックを倒すと、その方向にいる自チーム内野へ操作キャラクターを切り替えます。
- ジャンプ中でもシュートやパスができます。
- 相手チームの内野を全員倒すと勝利、自チームの内野を全員倒されると敗北です。

## ボタンの意味

自チームがボールを持っている場合:

| ボタン | 動作 |
| --- | --- |
| ボタン1 | 味方へパス |
| ボタン2 | 相手へシュート |
| ボタン3 | ボール保持者がジャンプ |

相手チームがボールを持っている場合:

| ボタン | 動作 |
| --- | --- |
| ボタン1 | 操作キャラがしゃがむ |
| ボタン2 | 操作キャラがキャッチ構え |
| ボタン3 | 操作キャラがジャンプ |

パスとシュートは、ボタンを押した瞬間の左スティックまたは方向キーの向きにいるターゲットを自動で選びます。方向入力がない場合は、基本的に相手方向または近い味方を狙います。

ボール保持中は、パス候補の味方にPマークがつきます。方向入力なしでボタン1を押すとPマークの味方へパスします。方向入力をしながらボタン1を押すと、上・下・右の指定ポジションへPマークが切り替わり、そのまま即パスします。

## ファイルの役割

| ファイル | 内容 |
| --- | --- |
| `index.html` | Canvasと各JavaScriptファイルの読み込み |
| `style.css` | 画面全体のレイアウトと16:9表示 |
| `game.js` | ゲーム状態、斜め見下ろしコート、カメラ、複数キャラ管理、勝敗処理 |
| `player.js` | キャラクターのデータ、移動、ジャンプ、被弾、頭上HP、描画 |
| `ball.js` | ボールの保持、パス、シュート、落下、跳ね返り |
| `cpu.js` | CPUチームの移動、投球、パス、回避、キャッチ判断 |
| `input.js` | キーボードとGamepad APIの入力管理 |

## 開発メモ

- 内部解像度は1280×720です。
- コートは旧版より横1.5倍、縦1.2倍の世界座標で管理しています。
- `game.js` の先頭にある `DEBUG_MODE` を `true` にすると、当たり判定や移動可能範囲を表示できます。
- キャラクター描画は現在Canvasの図形ですが、`Player.draw()` を差し替えることでPNG画像やスプライトに拡張できます。
- 必殺シュート、キャラクター選択、2人対戦などは、現在のチーム配列と入力処理を拡張して追加できます。
